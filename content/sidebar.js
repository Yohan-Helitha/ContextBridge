// ContextBridge — Sidebar UI Logic
// Renders the side drawer panel injected into the host page via Shadow DOM.
// Handles the full Capture Flow: scrape → summarize → edit → save handoff.
// Full implementation: Step 09

export function initSidebar(platform, shadowRoot) {
  console.debug('[ContextBridge] sidebar stub — platform:', platform);
}
